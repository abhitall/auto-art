# Supply chain security

Model artifact checks (e.g. SafeTensors validation, file scanning) are implemented in `auto_art.core.monitoring` and related utilities. Use in CI before loading `target.model_path`.

See `docs/automation/ci-cd.md` for pipeline placement.
