# NLP attacks

Semantic text perturbations (`SemanticPerturbationAttack`) in `auto_art.core.attacks.nlp.semantic_attack` — typo, synonym, and contextual perturbations for text classifiers.

## Orchestrator

Wire through custom scripts or extend the LLM/text pipeline; numpy-based evasion phases expect image/tabular tensors. See `tests/unit/test_nlp_attacks.py`.
