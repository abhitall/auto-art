# Warm-start evaluation

Incremental / cached evaluation helpers: `auto_art.core.warmstart`. Use to skip redundant attack–defence pairs when configs and artifacts are unchanged.

See unit tests in `tests/unit/test_warmstart.py` and orchestrator integration notes in `docs/automation/orchestrator.md`.
